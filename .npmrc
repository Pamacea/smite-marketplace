# npm configuration for SMITE marketplace
# Ensures proper handling of workspace dependencies

legacy-peer-deps=true
# Allow installing peer dependencies without strict version matching
# This is needed because SMITE uses a monorepo structure with workspace:*

auto-install-peers=true
# Automatically install peer dependencies
